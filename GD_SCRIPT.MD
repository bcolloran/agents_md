# Style Guide for GDScript

- Always use 2 spaces for indentation. Never use tabs.
- Use godot type hints for function parameters and return types wherever possible.
  - Prefer to use inferred (`:=`) types when possible, but add an explicit type hint if the Godot type will not be correctly inferred.